import {
    SET_ALERT,
    REMOVE_ALERT
} from '../actions/constants'
const initialState = []

export default function (state = initialState, action) {
    switch (action.type) {
        case SET_ALERT:

    }
}